<%= link_to "Add new Project", new_project_path, :class => "btn btn-success pull-right" %>
This is the projects list, 

<ul>
<% collection.each do | proj | %>
  <li><%= link_to proj.name, project_path(proj) %></li>
<% end %>
</ul>

